{
    "derive_component": {
        "prefix": "dc",
        "body": ["#[derive(Component)]"],
        "description": "#[derive(Component)]"
    },
    "derive_resource": {
        "prefix": "dr",
        "body": ["#[derive(Resource)]"],
        "description": "#[derive(Resource)]"
    }
}
